{
  "name": "33-cookie-allow-header-cookie",
  "cadishfile": "example.com {\n    cookie_allow sess\n\n    cache_key host path header:Cookie\n\n    cache_ttl default ttl 60s grace 1h\n\n    header +cache_status X-Cache\n}",
  "cases": [
    {
      "request": {
        "method": "GET",
        "host": "example.com",
        "path": "/page",
        "header": { "Cookie": ["sess=AAA; track=1"] }
      },
      "origin": {
        "status": 200,
        "header": { "Content-Type": ["text/html"] }
      },
      "cacheStatus": "MISS"
    }
  ]
}
